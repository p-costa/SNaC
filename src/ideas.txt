updthalo(idir,halo,nghost,var) ! and use var(1-nghost,...)
one single 'mom', unifying imp and explicit (more storage but fine)
one scalar transport routine
one output with MPI_CREATE_SUBARRAY accounting for the halo regions
